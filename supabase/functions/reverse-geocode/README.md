# reverse-geocode

This Supabase Edge Function now uses Google Geocoding API for reverse geocoding.

## Required secret

- `GOOGLE_GEOCODING_API_KEY`

## Deprecated secrets

The following legacy Nominatim secrets are no longer used:

- `NOMINATIM_USER_AGENT`
- `APP_REFERER`

